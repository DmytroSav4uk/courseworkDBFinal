import { MonthTitlePipe } from './month-title.pipe';

describe('MonthTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new MonthTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
